<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by PHP Project Wizard (PPW) 1.0.4 on Sun Feb 12 2:32:57 GMT 2012 -->

<project name="php-rlo" default="build">

    <path id="filesProcess">
<!--        <pathelement path="${basedir}/application/config" />-->
        <pathelement path="${basedir}/application/controllers" />
<!--        <pathelement path="${basedir}/application/core" />-->
<!--        <pathelement path="${basedir}/application/errors" />-->
<!--        <pathelement path="${basedir}/application/helpers" />-->
<!--        <pathelement path="${basedir}/application/hooks" />-->
<!--        <pathelement path="${basedir}/application/language" />-->
        <pathelement path="${basedir}/application/models" />
<!--        <pathelement path="${basedir}/application/views" />-->
    </path>
   
    <pathconvert targetos="unix" property="filesProcess.processed" refid="filesProcess" pathsep=" " />           
    <pathconvert targetos="unix" property="filesProcess.processed.comma" refid="filesProcess" pathsep="," />           

 <target name="clean" description="Clean up and create artifact directories">
     
  <delete dir="${basedir}/build/api"/>
  <delete dir="${basedir}/build/code-browser"/>
  <delete dir="${basedir}/build/coverage"/>
  <delete dir="${basedir}/build/logs"/>
  <delete dir="${basedir}/build/pdepend"/>

  <mkdir dir="${basedir}/build/api"/>
  <mkdir dir="${basedir}/build/code-browser"/>
  <mkdir dir="${basedir}/build/coverage"/>
  <mkdir dir="${basedir}/build/logs"/>
  <mkdir dir="${basedir}/build/pdepend"/>
 </target>

 <target name="phpunit" description="Run unit tests using PHPUnit and generates junit.xml and clover.xml">
  <exec executable="phpunit" failonerror="true">
   <arg line="-c ${basedir}/tests/phpunit.xml" />
  </exec>  
 </target>

 <target name="parallelTasks" description="Run the pdepend, phpmd, phpcpd, phpcs, phpdoc and phploc tasks in parallel using a maximum of 2 threads.">
     
  <parallel threadCount="2">
   <sequential>
    <antcall target="pdepend"/>
    <antcall target="phpmd"/>
   </sequential>
   <antcall target="phpcpd"/>
   <antcall target="phpcs"/>
   <antcall target="phpdoc"/>
   <antcall target="phploc"/>
  </parallel>
 </target>

 <target name="pdepend" description="Generate jdepend.xml and software metrics charts using PHP_Depend">
  <exec executable="pdepend">
   <arg line="--jdepend-xml=${basedir}/build/logs/jdepend.xml
              --jdepend-chart=${basedir}/build/pdepend/dependencies.svg
              --overview-pyramid=${basedir}/build/pdepend/overview-pyramid.svg
              ${filesProcess.processed.comma}" />
  </exec>
 </target>

 <target name="phpmd" description="Generate pmd.xml using PHPMD">
  <exec executable="phpmd">
   <arg line="${filesProcess.processed.comma}
              xml
              design,naming,unusedcode
			 --minimumpriority = 400
			 --ignore=pbcore.php
			 --reportfile ${basedir}/build/logs/pmd.xml" />
  </exec>
 </target>

 <target name="phpcpd" description="Generate pmd-cpd.xml using PHPCPD">
  <exec executable="phpcpd">
   <arg line="--log-pmd ${basedir}/build/logs/pmd-cpd.xml
              ${filesProcess.processed}" />
  </exec>
 </target>

 <target name="phploc" description="Generate phploc.csv">
  <exec executable="phploc">
   <arg line="--log-csv ${basedir}/build/logs/phploc.csv
             ${filesProcess.processed}" />
  </exec>
 </target>

 <target name="phpcs" description="Generate checkstyle.xml using PHP_CodeSniffer">
  <exec executable="phpcs" output="/dev/null">
   <arg line="--report=checkstyle
			  --standard=CodeIgniter
              --report-file=${basedir}/build/logs/checkstyle.xml
              ${filesProcess.processed}" />
  </exec>
 </target>

 <target name="phpdoc" description="Generate API documentation using PHPDocumentor">
  <exec executable="phpdoc">
   <arg line="-d ${filesProcess.processed.comma}" />
   <arg line="-t ${basedir}/build/api" />
  </exec>
 </target>

 <target name="phpcb" description="Aggregate tool output with PHP_CodeBrowser">
  <exec executable="phpcb">
   <arg line="--log    ${basedir}/build/logs
              --source ${basedir}
              --output ${basedir}/build/code-browser" />
  </exec>
 </target>

 <target name="build" depends="clean,parallelTasks,phpunit,phpcb">
     
 </target>
</project>